# Bericht — Aufgaben 1–5 in einem Rutsch
## Stand 2026-06-13 · Branch `aura-kruecke/2-vario-map` · Commits f2d43eb … e2be656

> Alle 5 Punkte umgesetzt, einzeln gebaut/committet, mit Stabilitäts- + Performance-Test am Schluss.
> Legende: ✅ fertig & getestet · 🧪 nur am Tisch prüfbar (Rest braucht Flug) · 📝 bewusster Folge-Schritt.

---

## 1. Konfigurator live verdrahtet ✅ (`f2d43eb`)
Die App-Einstellungen **wirken** jetzt, nicht nur gespeichert:
- **Einheiten** (`units.*`): neues `units.h` rechnet die **Anzeige** um (intern bleibt alles metrisch). **Cruise + Thermik**: Höhe (m/ft), Speed (km/h/mph/kt), Temperatur (°C/°F). Live bei jeder `units.*`-Änderung.
- **Pilot-Name** (`pilot.name`/`glider`): geht jetzt in den **IGC-Header** (statt hartcodiert „Ivo Eichenberger").
- **Backlight** (`display.backlight`): **persistent** — Menü-Toggle UND App schreiben ins Modell, beim Boot geladen. Default aus (E-Paper spart Strom).
- 📝 Offen: Vario-Einheit (ms/ftmin/kt), `display.brightness` (E-Paper hat keine Helligkeit), Totzone/Tonkurve (gespeichert, wirken noch nicht), Einheiten auf Ziel-/Schnitt-Screen.

## 2. M4 — Task-Wegpunkt-Navigation ✅🧪 (`63131d0`)
- `taskLoad()` liest `/tasks/<name>.json` (bis 24 WP); `taskTick()` spiegelt den aktuellen WP in `activeWP` → der **Ziel-Screen rechnet Peilung/Distanz** schon. Im Flug **schaltet beim Erreichen des WP-Radius automatisch weiter**.
- Aktiver Task **persistent** (`/tasks/active.txt`); ein per BLE hochgeladener Task (M3) wird **automatisch aktiv**; beim Boot geladen.
- Ziel-Screen zeigt **WP-Index** am Namen (z. B. „Hörnli 2/5").
- 🧪 Tisch: lädt sauber, kein Crash. Das **Weiterschalten** braucht einen Flug (oder GPS-Fix an WP-Position).

## 3. Luftraum-Eintritts-Warnung ✅🧪 (`3e4a1ff`)
- **Karte rendert Luftraum schon** (war im alten Bericht falsch als offen vermerkt). Neu: **Warnung**.
- `aspWarnTick()` prüft **im Flug** Punkt-in-Polygon + Vertikal-Band (`lower`/`upper` → Meter, ± `warn.buffer_v`) gegen alle aktiven Lufträume.
- **Bei Eintritt:** 3 Buzzer-Beeps + 2,5 s Warn-Screen (Klasse · Name · Floor–Ceiling). Gated über `warn.airspace`. **Am Boden kein Alarm** (kein Nervtöter).
- 🧪 Tisch: kompiliert/stabil. Echter Trigger braucht Flug in einen Luftraum (oder GPS-Fix dort). 📝 Folge: horizontaler Annäherungs-Puffer (`buffer_h`), Hindernis-Warnung.

## 4. FANET fertig ✅🧪 (`cf66f47`)
- **Name-Beacon** (Typ 2): sendet `fanet.pilot_name` ~alle 60 s im Flug → andere sehen deinen Namen.
- **Online-Flag**: `fanet.online_tracking` statt hartcodiert.
- **Landung → Ride/SOS** (Typ-3-Nachricht mit Name + Position): „RIDE bitte …" / „SOS HILFE …". **SOS armt FANET im Notfall**; Buzzer-Bestätigung.
- Alles über die **gleiche Doppel-Sicherung** (Gate D + `tx_enabled`). 🧪 On-Air-Validierung steht aus (zweite FANET-Gegenstelle / OGN).

## 5. Stabilität & Performance ✅ (`e2be656`)
- Toter Code raus (alte TON-TEST-Infra in `buzzer.h`).
- **`[PERF]`-Log** alle 5 s: max. Loop-Zeit + freier Heap + PSRAM.
- **Langtest 65 s, nach allen Features:**
  - **Kein Crash** (13 Mess-Zyklen).
  - **loopMax ~380 ms konstant** = der 1-Hz-E-Paper-Teilrefresh (erwartet, kein Ausreisser). BLE/FANET laufen im Hintergrund-Task, unbeeinflusst.
  - **Heap ~98 KB + PSRAM 6,68 MB konstant → kein Speicherleck.**
- Flash 26 %, RAM 28 % — viel Luft.

---

## Was du am Tisch prüfen kannst
- **#1 Einheiten:** in der App `units.alt=ft` setzen → Cruise/Thermik zeigen Fuss. Pilot-Name setzen → steht im nächsten IGC.
- **#1 Backlight:** Menü → Backlight togglen → übersteht Neustart.
- **#2 Task:** Task per App hochladen → Ziel-Screen zeigt 1. WP + „1/N".
- **#4:** Funk-Screen → FANET **SCHARF** → Landung simulieren ist schwer; aber Ride/SOS-Logik + Beep prüfbar, sobald ein Lande-Screen kommt.

## Bewusste Folge-Schritte (kein Stress)
- In-Flight-Tests: #2 WP-Weiterschalten, #3 Luftraum-Alarm, #4 FANET on-air.
- #1 Rest-Einheiten + Display-Helligkeit + Totzone/Tonkurve.
- #3 horizontaler Puffer + Hindernis-Warnung.
- BLE Phase-2 (Verschlüsselung+Bond) · BNO055 (nächste Woche).
